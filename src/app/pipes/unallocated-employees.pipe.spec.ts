import { UnallocatedEmployeesPipe } from './unallocated-employees.pipe';

describe('UnallocatedEmployeesPipe', () => {
  it('create an instance', () => {
    const pipe = new UnallocatedEmployeesPipe();
    expect(pipe).toBeTruthy();
  });
});
